homepage:
    pattern:  /
    defaults: { _controller: QaQuestionBundle:Default:index}

_items:
    pattern:  /{page}
    defaults: { _controller: QaQuestionBundle:Default:index }
    
question_show:
    pattern:  /question/{id}/{slug}
    defaults: { _controller: QaQuestionBundle:Default:show }

tag_show:
    pattern:  /tag/{slug}
    defaults: { _controller: QaQuestionBundle:Default:questionsByTag }